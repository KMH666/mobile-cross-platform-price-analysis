# 手机跨平台价格分析系统 — 使用文档

## 一、项目概述

本项目以 **618购物节** 为背景，爬取 **京东、淘宝（慢慢买）、抖音商城** 三个电商平台的 **手机** 商品数据，从价格分布、品牌销量、跨平台价差、折扣力度等多个维度进行数据分析，最后生成文本分析报告和5张可视化图表，并提供命令行交互式选购推荐功能。

### 项目结构

```
python期末项目/
├── config.py                  # 配置文件（爬虫URL、路径、参数等）
├── main.py                    # 主入口（依次执行爬虫→清洗→分析→可视化→推荐）
├── requirements.txt           # Python依赖包清单
│
├── crawlers/                  # 爬虫模块
│   ├── __init__.py
│   ├── sample_data.py         # 样本数据生成器（爬虫失败时自动使用）
│   ├── jd_crawler.py          # 京东爬虫（requests + BeautifulSoup）
│   ├── taobao_crawler.py      # 淘宝爬虫（通过"慢慢买"比价网站）
│   └── douyin_crawler.py      # 抖音爬虫（API + 网页解析，备选Selenium）
│
├── analysis/                  # 分析模块
│   ├── __init__.py
│   ├── clean.py               # 数据清洗（统一字段、去重、品牌提取）
│   ├── analyze.py             # 数据分析 + 命令行选购推荐
│   └── visualize.py           # 可视化（5张图表）
│
├── data/                      # 数据目录
│   ├── raw/                   # 三平台原始数据（CSV）
│   └── cleaned/               # 清洗后合并数据
│
├── charts/                    # 图表输出目录
│   ├── fig1_avg_price_bar.png           # 三平台平均价格对比柱状图
│   ├── fig2_price_distribution.png      # 三平台价格分布堆叠直方图
│   ├── fig3_brand_platform_price.png    # Top10品牌跨平台均价横向柱状图
│   ├── fig4_price_boxplot.png           # 三平台价格箱线图
│   └── fig5_price_vs_sales_scatter.png  # 价格vs销量散点图
│
├── 辅助代码/                  # 辅助工具（PPT/报告生成脚本）
│   ├── create_ppt.py          # 答辩PPT自动生成
│   ├── generate_report.py     # 实验报告PDF生成
│   └── code_snippets.txt      # 代码片段参考
│
└── 分析报告.txt               # 自动生成的文本分析报告
```

## 二、环境准备

### 2.1 安装Python

确保已安装 **Python 3.8+**，在命令行验证：

```bash
python --version
```

### 2.2 安装依赖包

在项目目录下运行：

```bash
pip install -r requirements.txt
```

依赖清单（`requirements.txt`）：

```
pandas>=1.5.0       # 数据处理
numpy>=1.24.0       # 数值计算
matplotlib>=3.7.0   # 图表绘制
seaborn>=0.12.0     # 图表美化
requests>=2.28.0    # HTTP请求
beautifulsoup4>=4.12.0  # HTML解析
lxml>=4.9.0         # 解析器
```

## 三、运行方法

### 3.1 一键运行（推荐）

在项目目录下执行：

```bash
python main.py
```

系统将依次执行：
1. **爬取数据** — 分别爬取京东、淘宝、抖音三平台（如失败自动使用样本数据）
2. **清洗数据** — 统一字段格式、提取品牌、去重
3. **分析数据** — 生成统计报告并保存为 `分析报告.txt`
4. **可视化** — 生成5张图表保存到 `charts/` 目录
5. **选购推荐** — 进入命令行交互模式，输入条件获取推荐

> **注意**：第5步需要交互式输入，在PowerShell中重定向输出时可能无法输入。建议在 **cmd** 或直接运行 `python main.py`。

### 3.2 分步运行

也可以单独运行各模块：

```bash
# 仅清洗已有数据
python -c "from analysis.clean import run; run()"

# 仅生成图表（需先有清洗后数据）
python analysis/visualize.py

# 仅查看选购推荐
python -c "from analysis.analyze import load_clean_data, interactive_filter; interactive_filter(load_clean_data())"
```

### 3.3 爬虫测试

单独测试各平台爬虫：

```bash
python crawlers/jd_crawler.py
python crawlers/taobao_crawler.py
python crawlers/douyin_crawler.py
```

## 四、选购推荐交互功能

运行 `main.py` 第五阶段会自动进入交互模式，示例：

```
请输入你的预算上限(元): 3000
请输入偏好的品牌(多个用逗号分隔，回车跳过): 小米,荣耀
是否需要5G/折叠屏等高端机型？(y/n): y

为你找到 15 款符合条件的手机：
排名 | 品牌   | 型号摘要           | 价格     | 平台 | 销量
1    | 小米   | 14 Ultra 5G手机... | ¥2,999  | 京东 | 3.2万
2    | 荣耀   | Magic6 Pro...      | ¥2,899  | 抖音 | 2.8万
...

推荐：在【抖音】购买【荣耀Magic6 Pro...】性价比最高！
```

## 五、输出结果说明

### 5.1 分析报告（分析报告.txt）

包含6个部分：
- **整体统计**：总商品数、均价、中位数、标准差、折扣率
- **跨平台价格对比**：三平台均价直观对比
- **折扣力度分析**：各平台平均折扣率
- **销量Top10品牌**：按总销量排序
- **同品牌跨平台价差**：同一品牌在不同平台的价差
- **性价比平台排行**：综合价格和销量评估

### 5.2 图表说明

| 文件名 | 图表类型 | 说明 |
|--------|----------|------|
| `fig1_avg_price_bar.png` | 分组柱状图 | 三平台价格均值和中间值对比 |
| `fig2_price_distribution.png` | 堆叠直方图 | 三平台在各价格区间的分布 |
| `fig3_brand_platform_price.png` | 横向柱状图 | Top10品牌跨平台价差 |
| `fig4_price_boxplot.png` | 箱线图 | 三平台价格离散程度 |
| `fig5_price_vs_sales_scatter.png` | 散点图 | 价格和销量关系，标注爆款 |

## 六、常见问题

**Q: 爬虫总是失败，只有样本数据？**  
A: 京东、淘宝、抖音均有反爬机制。这是正常现象，样本数据使用随机种子保证可复现，不影响分析流程的完整性和演示效果。

**Q: 如何修改搜索品类？**  
A: 修改 `config.py` 中的 `KEYWORD = "手机"` 为其他关键词即可。

**Q: 图表中文显示为方框？**  
A: 系统会自动尝试多个中文字体。如果仍显示异常，请在 `visualize.py` 的 `font_names` 列表中添加你系统中已安装的中文字体名称。

**Q: 如何增加每个平台的数据量？**  
A: 修改 `config.py` 中的 `PAGES_PER_PLATFORM` 增加爬取页数，以及 `sample_data.py` 中 `generate_products_for_platform` 的 `count` 参数。

## 七、实验报告提示

若需提交实验报告，建议包含以下部分：
1. **项目背景**（618购物节价格分析）
2. **技术架构**（模块划分、爬虫方案选型）
3. **数据采集**（三平台爬取策略、反爬应对措施）
4. **数据清洗**（字段统一、去重逻辑）
5. **数据分析**（核心发现，如抖音均价¥3,140最便宜、Apple跨平台价差¥1,280等）
6. **可视化展示**（附5张图表截图）
7. **选购推荐演示**（附交互运行截图）
8. **总结与结论**（验证"618是否真的便宜"）
